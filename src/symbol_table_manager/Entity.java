package symbol_table_manager;

/**
 * Every program entity that implements this interface says
 * you got to have a name(the pieces of info we're interested in about every program entity)
 */

public interface Entity {
    String getName();
}
